// Frontend constants
export const FRONTEND_URL = "http://localhost:3000";
export const REQUEST_TYPES = ["GET", "PUT", "POST"];

// URL constants
export const BASE_URL = '/';

// Backend constants
export const PORT: number = 8000;
export const BACKEND_ADDRESS = `http://localhost:${PORT}`;

// Conversion Constants
export const WAV_EXTENTION = '.wav';